# muilt-agent
他妈的模型不会第一区调度工具，而是优先去进行语言回复，所以要在提示语里面明确的要求他去调用工具解决问题

新建一个state，然后再要传输信息和接收信息的乙方都设置这个state，更新，访问。


在想人工在环审批的方式如何实现。
添加相应的memoryserver到存储到数据库里面，还有interput。
1.可以利用飞书的卡片的方式，嵌入按钮，然后点击向服务器发起请求，同时发送对应的收到审批请求时的therd_id，然后在服务器收到后，进行处理。
2.或者可以在建一个服务器，存储对应的审批请求，要审批的时候，跳转到这里，然后进行审批